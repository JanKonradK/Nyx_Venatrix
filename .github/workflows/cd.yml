name: Deploy to Production

on:
  push:
    branches: [ "main" ]
    paths-ignore:
      - '**.md'
      - 'docs/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push Backend
      uses: docker/build-push-action@v5
      with:
        context: ./services/backend
        push: true
        tags: ${{ secrets.DOCKERHUB_USERNAME }}/nyx-venatrix-backend:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Build and push Frontend
      uses: docker/build-push-action@v5
      with:
        context: ./services/frontend
        push: true
        tags: ${{ secrets.DOCKERHUB_USERNAME }}/nyx-venatrix-frontend:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Build and push Agent
      uses: docker/build-push-action@v5
      with:
        context: ./services/agent
        push: true
        tags: ${{ secrets.DOCKERHUB_USERNAME }}/nyx-venatrix-agent:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Build and push Analytics
      uses: docker/build-push-action@v5
      with:
        context: ./services/analytics
        push: true
        tags: ${{ secrets.DOCKERHUB_USERNAME }}/nyx-venatrix-analytics:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max

    # Optional: Deploy to VPS/Cloud
    # - name: Deploy to Server
    #   uses: appleboy/ssh-action@v1.0.0
    #   with:
    #     host: ${{ secrets.DEPLOY_HOST }}
    #     username: ${{ secrets.DEPLOY_USER }}
    #     key: ${{ secrets.DEPLOY_SSH_KEY }}
    #     script: |
    #       cd ~/Nyx_Venatrix
    #       git pull
    #       docker-compose pull
    #       docker-compose up -d --build
